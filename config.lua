return {
    -- Text scaling
    text_scale_mult = 1.18,

    -- Colour adjustments
    darken_background = 0.7,
    reduce_saturation = 0.65,
    reduce_outline = 0.55,
    contrast_boost = 1.2,

    -- Button sizing
    button_padding_boost = 1.4,
    button_minw_boost = 1.35,
    button_minh_boost = 1.35,
    button_scale_boost = 1.12,

    -- Spacing
    hud_spacing_boost = 1.35,
    blind_padding_boost = 1.35,
    sidebar_minw_boost = 1.15,

    -- Feature toggles
    boost_text = true,
    boost_buttons = true,
    boost_contrast = true,
    reduce_outlines = true,
    improve_blind_select = true,
    improve_hud_layout = true,
    darken_background_enabled = true,
    reduce_saturation_enabled = true,
}
